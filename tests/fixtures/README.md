# FIT fixture policy

Only synthetic FIT files or samples with explicit redistribution permission belong here. Record each fixture's generator/source, license, and intended assertion before committing it.

Personal ride files belong in `tests/private/`; that directory is ignored by Git. FIT files can contain exact locations, times, biometrics, and device identifiers.
